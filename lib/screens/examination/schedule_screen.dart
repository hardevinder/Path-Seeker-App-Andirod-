import 'package:flutter/material.dart';

import '../admin/role_feature_screens.dart';

class ExaminationScheduleScreen extends StatelessWidget {
  const ExaminationScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      buildExaminationFeatureScreen('schedule');
}
