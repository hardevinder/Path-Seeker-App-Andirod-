name: studentapp_new
description: "Fresh Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI & Charts
  cupertino_icons: ^1.0.6
  pie_chart: ^5.3.2
  fl_chart: ^0.64.0

  # Firebase & Auth (✅ Fixed Versions)
  firebase_core: ^2.32.0
  firebase_auth: ^4.20.0
  google_sign_in: ^6.2.1
  cloud_firestore: ^4.13.0
  firebase_messaging: ^14.7.4

  # HTTP & Storage
  dio: ^5.4.0
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2
  permission_handler: ^11.3.0
  flutter_file_dialog: ^3.0.2
  device_info_plus: ^10.1.0

  # Device & Utilities
  url_launcher: ^6.2.5
  mime: ^1.0.4
  open_filex: ^4.3.2

  # Media & UX
  flutter_sound: ^9.2.13
  oktoast: ^3.3.1
  image_picker: ^1.0.7

  # Date Formatting
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
